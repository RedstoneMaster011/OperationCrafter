{
    "name": "Print to Screen",
    "color": "#007acc",
    "group": "Display",
    "asm_code": "mov si, msg_{ID}\ncall print_string\njmp after_{ID}\nmsg_{ID} db '{TEXT}', 13, 10, 0\nafter_{ID}:",
    "inputs": [
        {
            "name": "ID",
            "default": "1"
        },
        {
            "name": "TEXT",
            "default": "Hello from Blocks!"
        }
    ],
    "req_funcs": [
        "print_string:\n    pusha\n    mov bh, 0\n.loop:\n    lodsb\n    or al, al\n    jz .done\n    mov ah, 0x0e\n    int 0x10\n    jmp .loop\n.done:\n    popa\n    ret"
    ]
}
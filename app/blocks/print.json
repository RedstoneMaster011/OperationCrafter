{
    "name": "Print to Screen",
    "color": "#007acc",
    "group": "Display",
    "asm_code": "mov si, .msg_unique\ncall print_string\njmp .after_data\n.msg_unique db '{TEXT}', 13, 10, 0\n.after_data:",
    "inputs": [
        {
            "name": "TEXT",
            "default": "Hello from Blocks!"
        }
    ],
    "req_funcs": [
        "print_string:\n    pusha\n.loop:\n    lodsb\n    or al, al\n    jz .done\n    mov ah, 0x0e\n    int 0x10\n    jmp .loop\n.done:\n    popa\n    ret"
    ]
}